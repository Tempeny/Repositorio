public static Mision buscarMision() {
    Scanner scanner = new Scanner(System.in);
    System.out.print("Ingrese el nombre de la misión a buscar: ");
    String nombreBuscado = scanner.nextLine();

    for (Mision m : misiones) {
        if (m.getNombre().equalsIgnoreCase(nombreBuscado)) {
            System.out.println("\nMisión encontrada:");
            m.logMision();
            return m;
        }
    }

    System.out.println("No se encontró ninguna misión con ese nombre.");
    return null;
}